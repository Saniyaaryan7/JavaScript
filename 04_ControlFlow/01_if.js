// Store the current temperature
const temperature = 41;

// Check if temperature is greater than 45
if (temperature > 45) {
    console.log("Temperature is greater than 45 degree");   // Runs when temp > 45
}
else {
    console.log("Less than 45 degree");  // Runs when temp < 45
}
